# Сайт Коледжу / College Website

Повноцінний веб-сайт коледжу, побудований на Ruby on Rails 7 з використанням Bootstrap Modern Business Template, підтримкою завантаження файлів на AWS S3 та Docker конфігурацією.

## 📋 Зміст

- [Опис проекту](#опис-проекту)
- [Технологічний стек](#технологічний-стек)
- [Вимоги до системи](#вимоги-до-системи)
- [Швидкий старт з Docker](#швидкий-старт-з-docker)
- [Локальна розробка без Docker](#локальна-розробка-без-docker)
- [Налаштування AWS S3](#налаштування-aws-s3)
- [Структура проекту](#структура-проекту)
- [Deployment](#deployment)

## 📖 Опис проекту

Сучасний веб-сайт коледжу з українською навігацією, що включає:

- **Головна сторінка** - загальна інформація про коледж
- **Новини** - система публікації новин з можливістю завантаження зображень
- **Розділ "Студенту"** - інформація для студентів, гуртки
- **Розділ "Абітурієнту"** - інформація про вступ, спеціальності
- **Публічна інформація** - офіційні документи та звіти
- **Структурні підрозділи** - інформація про відділення та кафедри
- **Моніторинг якості** - показники якості освіти
- **Система документів** - завантаження та зберігання файлів на AWS S3

## 🛠 Технологічний стек

- **Ruby** 3.2.3
- **Rails** 7.1.6
- **PostgreSQL** 15+
- **Redis** 7+
- **Bootstrap** 5 (Modern Business Template)
- **AWS S3** для зберігання файлів
- **Docker** & Docker Compose
- **Importmap** для JavaScript
- **Turbo** & Stimulus для інтерактивності

## 💻 Вимоги до системи

### Для Docker (рекомендовано):
- Docker Engine 20.10+
- Docker Compose 2.0+

### Для локальної розробки:
- Ruby 3.2.3
- PostgreSQL 15+
- Redis 7+
- Node.js 20+ (для asset compilation)
- Yarn 1.22+

## 🚀 Швидкий старт з Docker

### 1. Клонування репозиторію

```bash
git clone https://github.com/Vitaly-Fedonyuk/college-website.git
cd college-website
```

### 2. Налаштування середовища

Створіть файл `.env` на основі `.env.example`:

```bash
cp .env.example .env
```

Відредагуйте `.env` файл та встановіть необхідні змінні:

```env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=college_website_development
RAILS_MASTER_KEY=your_rails_master_key
```

### 3. Запуск додатку

```bash
# Побудова образів (використовує Dockerfile.dev для development)
docker-compose build

# Запуск контейнерів
docker-compose up -d

# Створення бази даних
docker-compose exec web bin/rails db:create

# Виконання міграцій
docker-compose exec web bin/rails db:migrate

# Завантаження тестових даних
docker-compose exec web bin/rails db:seed
```

### 4. Відкрийте браузер

Перейдіть за адресою: http://localhost:3000

> **Примітка:** PostgreSQL та Redis працюють у внутрішній мережі Docker і не виставляють порти назовні. Це запобігає конфліктам з локальними сервісами. Для доступу до бази даних використовуйте команди нижче.

### Корисні Docker команди

```bash
# Перегляд логів
docker-compose logs -f web

# Зупинка контейнерів
docker-compose down

# Перезапуск
docker-compose restart

# Виконання Rails команд
docker-compose exec web bin/rails console
docker-compose exec web bin/rails routes

# Підключення до бази даних
docker-compose exec db psql -U postgres -d college_website_development
```

## 🔐 Адмін-панель

### Вхід в адмін-панель

Після запуску додатку адмін-панель доступна за адресою: http://localhost:3000/users/sign_in

**Дані для входу (створені автоматично при `db:seed`):**
- **Email:** admin@college.edu
- **Пароль:** password123

### Можливості адмін-панелі

- **Управління новинами:**
  - Створення нових новин
  - Редагування існуючих новин
  - Видалення новин
  - Завантаження зображень
  - Попередній перегляд перед публікацією
  - Збереження як чернетки (без дати публікації)
  
- **Пагінація:**
  - Автоматична пагінація списків новин
  - 10 новин на сторінку для публічної частини
  - 20 новин на сторінку в адмін-панелі

### Створення додаткових адміністраторів

Для створення нового адміністратора використовуйте Rails console:

```bash
# Для Docker
docker compose exec web bin/rails console

# Для локальної розробки
rails console
```

В консолі:

```ruby
User.create!(
  email: 'newadmin@college.edu',
  password: 'securepassword',
  password_confirmation: 'securepassword'
)
```

## 💼 Локальна розробка без Docker

### 1. Встановлення залежностей

```bash
# Встановлення Ruby gems
bundle install

# Встановлення Node.js пакетів
yarn install
```

### 2. Налаштування бази даних

```bash
# Створення бази даних
rails db:create

# Виконання міграцій
rails db:migrate

# Завантаження тестових даних
rails db:seed
```

### 3. Запуск серверів

Відкрийте три термінали:

```bash
# Термінал 1: Rails сервер
rails server

# Термінал 2: CSS компіляція
yarn build:css --watch

# Термінал 3: Redis (якщо потрібно)
redis-server
```

Або використовуйте Foreman:

```bash
gem install foreman
foreman start -f Procfile.dev
```

## ☁️ Налаштування AWS S3

### 1. Створення S3 Bucket

1. Увійдіть в AWS Console
2. Перейдіть до S3
3. Створіть новий bucket
4. Налаштуйте CORS policy для вашого bucket

### 2. Отримання credentials

1. Створіть IAM користувача з правами на S3
2. Згенеруйте Access Key ID та Secret Access Key

### 3. Налаштування в додатку

Додайте credentials в `.env`:

```env
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_REGION=us-east-1
AWS_BUCKET=your-bucket-name
```

### 4. Тестування

```bash
# В Rails console
rails console

# Створення тестового документа з файлом
doc = Document.create(name: "Test", description: "Test file")
doc.file.attach(io: File.open('path/to/file.pdf'), filename: 'file.pdf')
doc.file.attached? # => true
```

## 📁 Структура проекту

```
college-website/
├── app/
│   ├── assets/
│   │   └── stylesheets/
│   │       ├── application.bootstrap.scss
│   │       └── modern-business.css      # Bootstrap Modern Business styles
│   ├── controllers/
│   │   ├── applicants_controller.rb     # Розділ "Абітурієнту"
│   │   ├── documents_controller.rb      # Завантаження файлів
│   │   ├── home_controller.rb           # Головна сторінка
│   │   ├── news_controller.rb           # Новини
│   │   ├── pages_controller.rb          # Статичні сторінки
│   │   └── students_controller.rb       # Розділ "Студенту"
│   ├── javascript/
│   │   ├── application.js
│   │   ├── scripts.js                   # Modern Business scripts
│   │   └── controllers/
│   ├── models/
│   │   ├── document.rb                  # Модель документів
│   │   └── news.rb                      # Модель новин
│   └── views/
│       ├── layouts/
│       │   └── application.html.erb     # Основний layout
│       ├── shared/
│       │   ├── _navigation.html.erb     # Українська навігація
│       │   └── _footer.html.erb         # Footer
│       ├── home/
│       ├── news/
│       ├── students/
│       ├── applicants/
│       ├── pages/
│       └── documents/
├── config/
│   ├── database.yml                     # Налаштування PostgreSQL
│   ├── storage.yml                      # Налаштування S3
│   └── routes.rb                        # Маршрути
├── db/
│   ├── migrate/                         # Міграції
│   └── seeds.rb                         # Тестові дані
├── docker-compose.yml                   # Docker Compose конфігурація
├── Dockerfile                           # Docker image конфігурація
└── .env.example                         # Приклад змінних середовища
```

## 🚢 Deployment

### Підготовка до deployment

1. **Налаштуйте production credentials:**

```bash
EDITOR="nano" rails credentials:edit --environment production
```

2. **Налаштуйте змінні середовища на production сервері**

3. **Виконайте міграції:**

```bash
RAILS_ENV=production rails db:migrate
```

4. **Прекомпілюйте assets:**

```bash
RAILS_ENV=production rails assets:precompile
```

### Docker deployment

```bash
# Побудова production image
docker build -t college-website:latest .

# Запуск з production конфігурацією
docker-compose -f docker-compose.production.yml up -d
```

### Heroku deployment

```bash
# Встановіть Heroku CLI
heroku create college-website-app

# Додайте PostgreSQL
heroku addons:create heroku-postgresql:mini

# Додайте Redis
heroku addons:create heroku-redis:mini

# Налаштуйте змінні
heroku config:set RAILS_MASTER_KEY=your_master_key
heroku config:set AWS_ACCESS_KEY_ID=your_key
heroku config:set AWS_SECRET_ACCESS_KEY=your_secret
heroku config:set AWS_REGION=us-east-1
heroku config:set AWS_BUCKET=your-bucket

# Deploy
git push heroku main

# Виконайте міграції
heroku run rails db:migrate
heroku run rails db:seed
```

## 📝 Ліцензія

Цей проект є відкритим програмним забезпеченням.

## 👥 Автори

- Vitaly Fedonyuk

## 🤝 Підтримка

Якщо у вас виникли питання або проблеми, створіть issue в GitHub репозиторії.
